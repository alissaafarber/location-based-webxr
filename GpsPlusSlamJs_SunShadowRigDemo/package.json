{
  "name": "gps-plus-slam-sun-shadow-rig-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Demo for the AppFramework Sun Shadow Rig module showing realistic sun-driven shadows.",
  "type": "module",
  "scripts": {
    "dev": "pnpm run build:framework && vite",
    "build": "pnpm run build:framework && tsc -p tsconfig.json && vite build",
    "build:framework": "pnpm --filter gps-plus-slam-app-framework run build",
    "preview": "vite preview"
  },
  "engines": {
    "node": ">=22.14.0"
  },
  "dependencies": {
    "gps-plus-slam-app-framework": "workspace:*",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@types/node": "^25.7.0",
    "@types/three": "^0.184.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.12"
  },
  "license": "Apache-2.0"
}
